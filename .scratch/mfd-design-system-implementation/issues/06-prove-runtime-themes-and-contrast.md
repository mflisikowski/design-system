# 06: Prove runtime themes and contrast

**What to build:** Demonstrate the generated theme contract in a rendered fixture that applies the three independent root axes, resolves system color scheme correctly, and measures approved semantic pairs on their actual backgrounds.

**Blocked by:** 05: Generate and validate token artifacts.

**Status:** ready-for-agent

- [ ] A rendered fixture covers all eight resolved contexts with correct root attributes and self-hosted fonts.
- [ ] Fixed and system color schemes work without client JavaScript being required for initial resolution.
- [ ] Foreground, boundary, focus, selection, and on-solid pairs pass recorded contrast checks or carry narrowly scoped approved exceptions.
- [ ] Browser-provided controls receive the correct early `color-scheme` value.
